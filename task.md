# Task: Backend & CMS Content Editor Integration

- [ ] Project Setup & Package Installations
  - [ ] Install backend dependencies (`express`, `@prisma/client`, `bcryptjs`, `jsonwebtoken`, `cors`, `dotenv`)
  - [ ] Install development dependencies (`prisma` CLI)
- [ ] Database Schema & Seeder Script
  - [ ] Create `prisma/schema.prisma` with `Setting`, `Service`, `Project`, `Inquiry`, and `Admin` models
  - [ ] Configure PostgreSQL with SQLite fallback capabilities
  - [ ] Create `prisma/seed.js` seeder script creating `admin` / `admin123` credentials and default landing settings
  - [ ] Run Prisma migrations & run seeder
- [ ] Express.js API Server Setup
  - [ ] Create `server/middleware/auth.js` JWT verification guard
  - [ ] Create `server/index.js` setting up Express ports and mounting API endpoints
  - [ ] Register `/api/settings` and `/api/admin/settings` routes
  - [ ] Register CRUD endpoints for Services, Projects, and Inquiries
- [ ] Frontend Proxy & Dynamic Bindings Integration
  - [ ] Configure `vite.config.js` with standard API rewrite proxy rules
  - [ ] Build new `AdminSettings.jsx` page inside CMS to edit landing contents dynamically
  - [ ] Connect `GameHUD.jsx` navigations with settings tab
  - [ ] Bind visitor `Home.jsx` to fetch stats, class, bio, and WhatsApp targets dynamically
  - [ ] Bind Admin CRUD panels to fetch live database records instead of static mock arrays
- [ ] Verification & Client Delivery
  - [ ] Verify production compilation (`npm run build`)
  - [ ] Validate server endpoints and login JWT cookies
  - [ ] Verify dynamic landing page editing manual test
